<!-- app/views/videos/_feed.html.erb -->
<div class="row">
  <div class="large-4 columns">
    <% @videos.last(4).each do |video| %>
      <%= video.title %></br>
      <%= video.url %></br>

    <% end %>
  </div>
</div>